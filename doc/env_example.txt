AZURE_OPENAI_API_KEY = "<put API key here>"
AZURE_OPENAI_ENDPOINT = "<Azure OpenAI endpoint>"
OPENAI_API_VERSION = "<Azure OpenAI API version>"
MODEL_NAME = "<Azure OpenAI model name>"
TOKENIZERS_PARALLELISM = False

# --- User accounts & workspace routing (new in v1) ---------------------------
# WORKSPACES = finance,hr          # categories; each maps to data/<category> + a scoped index
# DATA_DIR = <project>/data        # root folder holding one subfolder per workspace
# AUTH_SECRET = ""                 # JWT signing secret (required in prod; ephemeral if unset)
# TOKEN_TTL_SECONDS = 604800       # session token lifetime (default 7 days)
# USERS_DB = <index>/users.sqlite3 # where accounts live (default: .index/users.sqlite3)

# --- Admin / operations -------------------------------------------------------
# API_KEY = ""                    # if set, all /admin* and /ingest* endpoints require `Authorization: Bearer <API_KEY>`
#                                 # (admin console at /admin: uploads, documents, workspaces, users) and enables the
#                                 # workspace registry at <INDEX_DIR>/workspaces.sqlite3 (custom workspaces + label overrides)
# CORS_ORIGINS = ""               # comma-separated allowed browser origins (empty = no cross-origin access)
# RATE_LIMIT_REQUESTS = 60        # max requests per window per IP
# RATE_LIMIT_WINDOW_SECONDS = 60
# AZURE_OPENAI_TIMEOUT = 120      # seconds per Azure call
# OPENAI_MAX_RETRIES = 3
# MAX_GENERATION_TOKENS = 600     # answer length cap (bounds latency + cost)
# CANDIDATE_K = 30                # candidates fed to reranker/MMR
# RERANKER_ENABLED = 1            # 0 disables the cross-encoder rerank step
# MMR_LAMBDA = 0.5                # 0 disables MMR (1 = relevance only)
# INDEX_DIR = .index              # where the vector stores + sessions + users live (use a persistent path on PaaS)
# MAX_RETAINED_SESSIONS = 500     # chat sessions kept (oldest auto-pruned)
# DOCLING_ENABLED = 1             # 0 disables Docling (plain-text only)
# DOCLING_OCR = 1                 # 0 disables OCR for scanned PDFs/images
# DOCLING_ASR = 1                 # 0 disables audio/video transcription
# DOCLING_ASR_MODEL = WHISPER_TURBO   # any docling asr_model_specs name
# DOCLING_VET_DIR = .docling_vet  # cleaned Markdown saved before ingest (gitignored; 0/false disables)
# PORT = 8000                     # uvicorn port (platforms like App Service inject this)
# LOG_LEVEL = INFO