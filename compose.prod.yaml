services:
  api:
    image: ${ESG_IMAGE:-ghcr.io/ridwanlekan/esg-related-chatbot:latest}
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./data:/app/data
      - esg-index:/app/.index
    restart: unless-stopped

volumes:
  esg-index: